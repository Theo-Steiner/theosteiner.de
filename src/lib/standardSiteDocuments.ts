import documentUris from '../content/standard-site-documents.json';

/** Standard.site document AT-URIs for non-Markdown detail pages. */
export const STANDARD_SITE_DOCUMENT_URIS: Record<string, string> = documentUris;
