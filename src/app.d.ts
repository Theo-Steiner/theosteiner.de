// Opt into @svebcomponents/atproto.comments' generated Svelte template types,
// so <atproto-comments> is checked (attributes + events) like any other element.
import '@svebcomponents/atproto.comments/svelte';

// See https://svelte.dev/docs/kit/types#app.d.ts
// for information about these interfaces
declare global {
	namespace App {
		// interface Error {}
		// interface Locals {}
		// interface PageData {}
		// interface PageState {}
		// interface Platform {}
	}
}

export {};
